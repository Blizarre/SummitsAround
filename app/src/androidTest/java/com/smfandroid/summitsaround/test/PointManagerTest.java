package com.example.summitsAround.test;

import com.example.summitsAround.*;

import android.test.AndroidTestCase;

public class PointManagerTest extends AndroidTestCase {

/*	public void testGetPointsForLocation() {
		PointManager pManag = new PointManager();
		assertEquals("At init should be empty", 0, pManag.GetPointsForLocation(null).size());
		
		assertEquals("At init should be empty", 0, pManag.GetPointsForLocation(null).size());
				
	}*/

}
